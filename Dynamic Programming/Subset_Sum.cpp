#include <iostream>

using namespace std;

int ss(int arr[], int n, target) {
	if(target == 0 ||)
	int flag[n + 1][n + 1];

}

int main() {
	int n;
	cout<<"Input n: ";
	cin>>n;
	int arr[n];
	cout<<"Input array: \n";
	for(int i = 0; i < n; i++) {
		cin>>arr[i];
	}
	int target;
	cout<<"Input target: "; cin>>target;
	ss(arr, n, target);
}